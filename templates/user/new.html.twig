{% extends 'base.html.twig' %}

{% block title %}New User
{% endblock %}

{% block body %}
	<h1 class="text-2xl font-bold mb-4 text-center mt-4">Create new User</h1>

	{{ include('user/_form.html.twig') }}

	<div class="mt-4 flex justify-around">
		<a href="{{ path('app_user_index') }}" class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded">Back to list</a>
	</div>
{% endblock %}
