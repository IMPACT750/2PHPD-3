{% extends 'base.html.twig' %}

{% block title %}Edit Game
{% endblock %}

{% block body %}
	<h1 class="text-2xl font-bold mb-4 text-center mt-4">Edit Game</h1>

	{{ include('game/_form.html.twig', {'button_label': 'Update'}) }}

	<div class="flex justify-center">
		<a href="{{ path('app_game_index') }}" class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded">
			Back to list
		</a>
	</div>

	<div class="flex justify-center mt-4">
		{{ include('game/_delete_form.html.twig') }}
	</div>
{% endblock %}
